[project]
name = "agent-substrate"
version = "0.1.0"
description = "Guards the agent substrate's portability invariants (repo tooling, not published)"
requires-python = ">=3.10,<4"
dependencies = []

[dependency-groups]
dev = [
  "pytest>=8.2.2,<9",
]

[tool.uv]
package = false
